<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/via_menu_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/dialog_via_menu_bg_dark"
    android:paddingTop="16dp"
    android:paddingBottom="12dp"
    android:paddingStart="8dp"
    android:paddingEnd="8dp">

    <!-- Swipeable ViewPager with 2 pages of actions -->
    <androidx.viewpager.widget.ViewPager
        android:id="@+id/via_menu_view_pager"
        android:layout_width="match_parent"
        android:layout_height="160dp" />

    <!-- Bottom Action Bar -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_marginTop="10dp"
        android:paddingStart="12dp"
        android:paddingEnd="12dp">

        <!-- Exit / Power Button (Left) -->
        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/btn_via_exit"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="6dp"
            android:src="@drawable/ic_via_power"
            android:clickable="true"
            android:focusable="true" />

        <!-- Real Page Dots Indicator (Center) -->
        <LinearLayout
            android:id="@+id/dots_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:orientation="horizontal"
            android:gravity="center">

            <TextView
                android:id="@+id/dot_1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="●"
                android:textSize="13sp"
                android:padding="4dp" />

            <TextView
                android:id="@+id/dot_2"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="○"
                android:textSize="13sp"
                android:padding="4dp" />
        </LinearLayout>

        <!-- Close Button (Right) -->
        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/btn_via_close"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="6dp"
            android:src="@drawable/ic_via_chevron_down"
            android:clickable="true"
            android:focusable="true" />
    </RelativeLayout>

</LinearLayout>
